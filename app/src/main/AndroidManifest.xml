<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cn.walkpast.horizon">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_GPS"/>
    <uses-permission android:name="android.permission.ACCESS_ASSISTED_GPS"/>
    <uses-permission android:name="android.permission.ACCESS_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_LOGS"/>

    <application
        android:name="cn.walkpast.horizon.app.ApplBase"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".competence.NormalActivity"></activity>
        <activity android:name=".competence.RedirectedActivity"></activity>
        <activity android:name=".competence.VideoPlayActivity"></activity>
        <activity android:name=".competence.MapActivity"></activity>
        <activity android:name=".competence.ImageDownloadActivity"></activity>
        <activity android:name=".competence.FileDownloadActivity"></activity>
        <activity android:name=".competence.DeeplinkActivity"></activity>
        <activity android:name=".competence.PaymentActivity"></activity>
        <activity android:name=".competence.PatternlessActivity"></activity>

        <!-- -->
        <activity android:name=".competence.BasicFunActivity"></activity>

        <!-- -->
        <activity android:name=".AboutUsActivity"></activity>


        <service
            android:name="cn.walkpast.download.DownLoadService"
            android:process=":horizon_download"></service>

        <!-- FileProvider配置访问路径，适配7.0及其以上 -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <!-- authorities：app的包名.fileProvider-->
            <!--grantUriPermissions：必须是true，表示授予 URI 临时访问权限-->
            <!-- exported：必须是false-->
            <!--resource：中的@xml/provider_paths是我们接下来要添加的文件-->
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>

</manifest>